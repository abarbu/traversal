;;;  -*- Hen -*-

(use setup-helper-mod)

(verify-extension-name "traversal")

(setup-shared-extension-module 'traversal (extension-version "1.4")
  #:inline? #t #:types? #t
  #:compile-options '(-O3 -d2))
